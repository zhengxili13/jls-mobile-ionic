android:
ng build --configuration=production
npx cap sync android
# For Code Push / OTA updates with Capacitor, use a Capacitor-compatible solution (e.g. App Center SDK or a community plugin).

ios:
ng build --configuration=production
npx cap sync ios
# For Code Push / OTA updates with Capacitor, use a Capacitor-compatible solution (e.g. App Center SDK or a community plugin).
